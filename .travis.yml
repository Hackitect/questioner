sudo: required

dist: xenial

language: python

python:
  - "3.5"

script: 
 - pytest --cov=app/
 - coveralls

virtualenv:
  system_site_package: true

install:
- pip install -r requirements.txt

after_success:
- coveralls

after_script: cd ~